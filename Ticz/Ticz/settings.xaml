<Page
    x:Class="Ticz.AppSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ticz"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:AppSettings x:Key="appSettings"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Uid="Settings" Grid.Row="0" Grid.Column="0" Style="{ThemeResource TitleTextBlockStyle}" Margin="10,10,10,0" TextWrapping="Wrap" Text="Settings" VerticalAlignment="Center"/>
        <Grid Grid.Row="1" HorizontalAlignment="Stretch" Margin="20,0,0,0">
            <StackPanel Orientation="Vertical">
                <TextBlock   Text="IP Address"/>
                <TextBox     Text="{Binding ServerIP, Mode=TwoWay}"/>
                <TextBlock   Text="Port" VerticalAlignment="Center"/>
                <TextBox     Text="{Binding ServerPort, Mode=TwoWay}" />
                <TextBlock   Text="Username"/>
                <TextBox     Text="{Binding Username, Mode=TwoWay}"/>
                <TextBlock   Text="Password"/>
                <PasswordBox Password="{Binding Password, Mode=TwoWay}"/>
                <Button      Content="Test Connection" Command="{Binding TestConnectionCommand}"/>
                <TextBlock   Text="{Binding TestConnectionResult}"/>
                <ProgressBar IsIndeterminate="{Binding TestInProgress}" Background="Transparent"/>
                <TextBlock   Text="Timeout"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0"  Minimum="4" Maximum="20" Value="{Binding Source={StaticResource appSettings}, Path=TimeOut, Mode=TwoWay}" SmallChange="1" />
                    <TextBlock Margin="30,0,0,0" VerticalAlignment="Center" Grid.Column="1" Text="{Binding TimeOutText, Mode=TwoWay}"/>
                </Grid>
            </StackPanel>
        </Grid>

    </Grid>
    <Page.BottomAppBar>
        <AppBar IsOpen="True" IsSticky="True">
            <StackPanel Orientation="Horizontal">
                <!--<CommandBar>
                    <CommandBar.SecondaryCommands>
                        <AppBarButton Name="settings" Icon="Setting" Label="settings" Visibility="Visible" Command="{Binding GoToSettingsCommand}"/>
                    </CommandBar.SecondaryCommands>
                </CommandBar>-->
                <AppBarButton Name="Back" Icon="Back" Label="Back" Command="{Binding NavigateBackCommand}"/>
            </StackPanel>
        </AppBar>
    </Page.BottomAppBar>
</Page>
